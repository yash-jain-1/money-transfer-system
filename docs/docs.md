# Docs

1. For connecting the onlie db through a docker host ->

```bash
docker run --rm -it mysql:8.0 mysql -h money-transfer-db-money-transfer-system-1.e.aivencloud.com -P 19878 -u avnadmin -p --ssl-mode=REQUIRED defaultdb
```
